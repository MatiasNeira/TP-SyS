tf=20;
w0=20;
wf=2000;
Fs=44100;
m= Fs*tf
t = linspace(0,tf,m);

subplot(2,1,1);
Xt=sineSweep(tf,w0,wf);
plot(Xt)

subplot(2,1,2);
Bt=inverseFilter(tf,wf,w0,t).*Xt;
plot(Bt)

sound(y,Fs);
audiowrite('sinesweep.wav',y,Fs);